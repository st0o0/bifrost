# Interface / config file name -> /etc/wireguard/<name>.conf
BIFROST_INTERFACE=wg0
# Seconds between DDNS re-resolve runs (0 disables)
BIFROST_RERESOLVE_INTERVAL=30
# Healthcheck: max age of the newest handshake before "unhealthy" (seconds)
BIFROST_HEALTH_MAX_HANDSHAKE_AGE=180
