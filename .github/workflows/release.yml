name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    uses: st0o0/github-workflows/.github/workflows/go-release.yml@main
    with:
      image-name: ghcr.io/st0o0/bifrost
      image-description: "Minimal native-Go WireGuard client that keeps dependents tunneled across DDNS endpoint IP changes without a netns teardown"
    permissions:
      contents: write
      pull-requests: write
      packages: write
      id-token: write
